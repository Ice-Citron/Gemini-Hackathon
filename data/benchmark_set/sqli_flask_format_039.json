{
  "id": "sqli_flask_format_039",
  "type": "sqli",
  "subtype": "flask_format",
  "vulnerable_code": "import sqlite3\nfrom flask import Flask, request, jsonify\n\napp = Flask(__name__)\n\n@app.route('/login')\ndef get_user():\n    term = request.args.get('term', '')\n    # VULNERABILITY: String format in SQL query\n    query = \"SELECT * FROM staff WHERE email = '{}'\".format(term)\n    conn = sqlite3.connect('database.db')\n    cursor = conn.cursor()\n    cursor.execute(query)  # <--- SQL INJECTION HERE\n    results = cursor.fetchall()\n    conn.close()\n    return jsonify(results)\n\nif __name__ == '__main__':\n    app.run(debug=True, port=5000)\n",
  "secure_code": "import sqlite3\nfrom flask import Flask, request, jsonify\n\napp = Flask(__name__)\n\n@app.route('/login')\ndef get_user():\n    term = request.args.get('term', '')\n    # SECURE: Parameterized query\n    query = \"SELECT * FROM staff WHERE email = ?\"\n    conn = sqlite3.connect('database.db')\n    cursor = conn.cursor()\n    cursor.execute(query, (term,))  # <--- PARAMETERIZED\n    results = cursor.fetchall()\n    conn.close()\n    return jsonify(results)\n\nif __name__ == '__main__':\n    app.run(debug=True, port=5000)\n",
  "exploit_command": "curl '/login?term=admin'--",
  "param_name": "term",
  "route": "/login",
  "generated_at": "2026-01-17T18:04:33.586367"
}